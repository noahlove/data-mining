# Homework 3 Project

```{r}
library(caret)
library(tidyverse)
```


```{r include=FALSE}
tweets_raw <- read_csv("Datasets/non_retweets_dc_inaug_steal.csv")
dim(tweets_raw)
```


```{r}
#clean code
tweets <- as_tibble(tweets_raw)

tweets_raw
```

